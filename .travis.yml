#sudo: false
sudo: required
addons:
  apt:
    packages:
      - build-essential
      - ikiwiki
install:
  - sudo apt-get install ikiwiki
  - openssl aes-256-cbc -K $encrypted_97fba1a3df90_key -iv $encrypted_97fba1a3df90_iv -in .travisdeploykey.enc -out .travisdeploykey -d
  - chmod go-rwx .travisdeploykey
  - eval `ssh-agent -s`
  - ssh-add .travisdeploykey
  - git config --global user.name "Travis-CI"
  - git config --global user.email "noreply@travis-ci.org"
  - COMMIT_MESSAGE="Publishing site on `date "+%Y-%m-%d %H:%M:%S"` from
    `git log -n 1 --format='commit %h - %s'`"
script:
  #- set -e
  - ikiwiki --rebuild . _site
  - git clone --bare -b gh-pages git@github.com:zmughal/ikiwiki-travis.git _site/.git
  - cd _site
  - git config core.bare false
  - git add -f .
  - 'git commit -m "${COMMIT_MESSAGE}"'
  - git push -f git@github.com:zmughal/ikiwiki-travis.git gh-pages:gh-pages

branches:
  only:
    - master
